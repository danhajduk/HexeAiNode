Workflow skill: $synthia-workflow 
(/home/dan/.codex/skills/synthia-workflow/SKILL.md)

## Codex Execution Directive

You are acting as the Synthia development agent.

Your job is to implement tasks listed in this file sequentially.

Rules:

1. Work through tasks in numerical order.
2. Continue executing tasks until ALL tasks are completed.
3. Do not stop after partial implementation.
4. Do not return summaries unless every task is finished.
5. If a task cannot be completed:
   - document the blocker
   - implement everything else that is possible
   - continue to the next task.

Execution behavior:

- Read the repository before modifying code.
- Follow existing architecture and coding conventions.
- Update or create files as required.
- Run tests or validation where possible.
- Commit after each completed task.

Completion criteria:

A task is considered complete only when:
- implementation exists in the codebase
- it compiles or runs without breaking existing features
- any required documentation is updated

Persistence rule:

Continue working autonomously until all tasks in this document are completed.
Do not stop early.

---

## Core Documentation Authority Rule (Golden Docs)

The **Core documentation (“golden docs”) is the authoritative specification** for all Core-owned contracts and behaviors.

When implementing tasks, the agent **must compare the repository implementation against the golden docs**.

### Rule 1 — Core authority
If there is a mismatch between **AI-Node code** and **Core documentation**:

- **Core documentation wins**
- Update the AI-Node implementation to match Core behavior
- Do NOT redefine or override Core contracts locally.

### Rule 2 — Missing Core-owned specification

If required data, behavior, or contract is **missing from Core documentation** and the item is **Core-owned**:

1. Create a mismatch report in:
/home/dan/Projects/Synthia/docs/reports


Suggested filename:
ai-node-mismatch-task-<task-number>.md

2. Document clearly:

- what is missing
- where it was discovered
- what implementation depends on it
- why it must be defined by Core

3. **Stop execution immediately**.

No further tasks may be implemented until the Core documentation is updated.

### Rule 3 — Missing Node-owned specification

If required data or behavior is **missing from Core documentation**, but the item is **Node-owned**:

1. Create a mismatch report in:
/home/dan/Projects/Synthia/docs/reports

2. Document:

- what is missing
- why the responsibility belongs to the node
- the local implementation decision

3. Implement the missing functionality **locally in AI-Node**.

4. Continue executing remaining tasks.

### Rule 4 — No silent contract drift

The agent must **never silently invent or redefine Core-owned contracts**.

All divergences between:

- code
- architecture
- documentation

must be reported using the mismatch report process.

### Rule 5 — Mismatch reporting is mandatory

Any discovered divergence between **Core documentation and AI-Node implementation** must be documented.

No undocumented behavior is allowed.

---

Do not edit above this line:
#######################################################################

